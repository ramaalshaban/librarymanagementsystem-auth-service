const express = require("express");

// AuthShareToken Db Object Rest Api Router
const authShareTokenRouter = express.Router();

// add AuthShareToken controllers

module.exports = authShareTokenRouter;
